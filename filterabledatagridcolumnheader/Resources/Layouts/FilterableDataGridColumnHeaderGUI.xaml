<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"    
             mc:Ignorable="d">
    <Border x:Name="ControlBorder" BorderBrush="Black" BorderThickness="1">
        <Grid x:Name="ControlGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ControlTextBox" BorderThickness="0" Background="Transparent"/>
            <ToggleButton x:Name="ControlToggle" Grid.Column="1" HorizontalAlignment="Right"  VerticalAlignment="Bottom" Width="15" Height="15"/>
        </Grid>
    </Border>
</UserControl>
